// Same search experience as the Search tab, reached instead as a modal
// overlay from Home's floating search icon — reuses the tab's component
// directly rather than duplicating its filtering/tracking logic.
export { default } from "./(tabs)/search";
