node_modules/
.expo/
dist/
web-build/
.env
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.env.local

# backend/ is its own Node project with its own .gitignore (node_modules,
# .env, uploads/*, *.log) — nothing backend-specific needed here.

# pnpm's local package store — cache, not project content
.pnpm-store/

# Claude Code local tool-permission state — machine/session-specific, not project content
.claude/settings.local.json
